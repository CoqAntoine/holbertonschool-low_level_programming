#include <stdio.h>
#include "dog.h"

{
	if (d == NULL)
		return;

	printf("Name: %s\nAge: %f\nOwner: %s", )
	if (d.name == NULL)
		printf("(nil)\n")
	else
		printf("%s\n", d.name)
	if (d.age == NULL)
		printf("(nil)\n")
	if (d.owner == NULL)
		printf("(nil)\n")

}