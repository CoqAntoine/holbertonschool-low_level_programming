#include <stdio.h>

/**
 * mul - check the code
 *
 * @a : entier a verifier
 *
 * @b : entier a verifier
 *
 * Return: retourne 1 si digit, 0 sinon
 */

int mul(int a, int b)
{
	int multiple = a * b;

	return (multiple);
}
