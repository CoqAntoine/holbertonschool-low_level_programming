#include <stdio.h>
#include "main.h"

/**
 * print_alphabet_x10 - print the alphabet
 *
 * Return: Always 0.
 */

int _islower(int c)
{
	char c;

	if (c >= 'a' && c <= 'z')
	        return (1);
	else
		return (0);
}
